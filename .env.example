# Optional: Serverless-Endpunkt für echte KI-Antworten im Chatbot.
# Leer lassen, um die eingebaute Wissensbasis zu nutzen (kein Backend nötig).
# Beispiel siehe README ("Optional: unbegrenzte KI-Antworten").
VITE_CHAT_API_URL=
